#!/bin/bash

set -x

brew update
brew upgrade

rustup self update
rustup self upgrade-data

rustup update
rustup upgrade

cargo install-update -a

sheldon -v lock

sudo gem update

pip-review --verbose --auto --continue-on-fail --user

